export type ImageBannerProps = {
    imageSrc: string;
    animalId:string;
}

export type AnimalHeaderProps = {
    animalName:string;
    location:string;
    date:string;
}

export type AnimalInformationProps = {
    text:string
}